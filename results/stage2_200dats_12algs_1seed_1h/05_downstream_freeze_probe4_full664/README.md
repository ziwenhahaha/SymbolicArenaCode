# 已迁移到 Stage 3

这里原来放的是 `Probe-4 full-664` 下游验证结果：

```text
664 datasets × 4 probes × 3 seeds × 1h = 7968 runs
```

这部分属于第三阶段，不再保留在 stage2。

请看：

```text
../../stage3_664dats_4probes_3seeds_1h/
```

stage2 只保留 `Candidate-200 × 12 algorithms × 1 seed × 1h` 的校准结果和 Probe-4 选择依据。
